[project]
name = "bluesky-discord-webhook-feed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "atproto>=0.0.61",
    "dotenv>=0.9.9",
]
